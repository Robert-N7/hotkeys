<?php

namespace TestFiles;

class Test implements
{
    function __construct() {
        foreach($arr as $k => $item) {
            $foo = 'foo';
            echo $foo . PHP_EOL;
            $foo = 'bar';
            $this->animals = [
                0 => 'Cat',
                1 => 'Dog',
                2 => 'Sheep'
            ];
            $sheep = $this->animals[2]
            foreach($sheep as $k => $x) {
                echo $x ;
}

        }

        foreach($arr as $k => $item) {
    // todo
}

    }
}
